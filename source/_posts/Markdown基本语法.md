---
title: Markdown 基本语法指南
date: 2025-12-18 15:16:00
tags: 
  - Markdown
  - 教程
categories: 
  - 技术文档
---

最后有彩蛋！！！

<!-- more -->

Markdown 是一种轻量级标记语言，用于格式化纯文本，使其易于转换为HTML。以下是常用语法及编辑器操作提示（以 [Typora](https://typora.io/#feature) 等常见编辑器为例）：

## 标题

**语法：**

```
# 一级标题
## 二级标题
### 三级标题
#### 四级标题
##### 五级标题
###### 六级标题
```



**编辑器操作：**

- **快捷键**：`Ctrl + 1` 到 `Ctrl + 6` 可以快速将当前行设置为对应的1-6级标题。
- **操作**：行首输入 `#` 号后跟一个空格，再输入标题内容。

## 文本样式

**语法：**

```
**粗体文本** 或 __粗体文本__
*斜体文本* 或 _斜体文本_
***粗斜体文本***
~~删除线文本~~
```



**编辑器操作：**

- **粗体**：选中文字，按 `Ctrl + B`。
- **斜体**：选中文字，按 `Ctrl + I`。
- **删除线**：选中文字，按 `Alt + Shift + 5`（部分编辑器适用）。

## 列表

### 无序列表

**语法：**

```
- 项目一
- 项目二
  - 子项目一 (按Tab键缩进)
  - 子项目二
* 另一种方式
+ 第三种方式
```



**编辑器操作：**

- **新建**：行首输入 `-`、`*` 或 `+` 后跟一个空格，然后输入内容，按 `Enter` 会自动创建下一个列表项。
- **结束**：连续按两次 `Enter` 或按 `Backspace` 删除自动生成的列表符号即可退出列表。

### 有序列表

**语法：**

```
1. 第一项
2. 第二项
   1. 子项一 (按Tab键缩进)
   2. 子项二
```



**编辑器操作：**

- **新建**：行首输入 `1.` 后跟一个空格，然后输入内容。编辑器会自动编号后续项目。
- **结束**：连续按两次 `Enter` 或按 `Backspace` 删除自动生成的编号即可退出列表。

## 链接和图片

**语法：**

```
[链接文字](https://example.com)
![图片描述](图片地址.jpg)
```



**编辑器操作：**

- **链接**：选中文字，按 `Ctrl + K`，然后输入网址。
- **图片**：直接拖拽图片到编辑器（需编辑器支持），或按上述语法手动输入。

## 引用

**语法：**

```
> 引用文本
> 多行引用
>
> > 嵌套引用
```



**编辑器操作：**

- **新建**：行首输入 `>` 后跟一个空格。
- **结束**：连续按两次 `Enter` 即可退出引用块。

## 代码

### 行内代码

**语法：**

```
使用 `printf()` 函数
```



**编辑器操作：**

- 选中文字，按 `Ctrl + Shift + ``（部分编辑器适用），或在代码前后输入反引号 ```。

### 代码块

**语法：**

<pre> ```python def hello(): print("Hello, World!") ``` </pre>

或使用缩进（四个空格或一个制表符）：

```
    这里是一个代码块
    使用四个空格缩进
```



**编辑器操作：**

- 输入 ````` 后跟语言名称（如 ````python`），然后按 `Enter` 即可创建一个代码块。在代码块内，Tab键用于缩进，而不是跳到下一个输入框。
- 在支持代码块的编辑器中，代码通常会有语法高亮。

## 表格

**语法：**

```
| 表头1 | 表头2 | 表头3 |
|-------|-------|-------|
| 单元格1 | 单元格2 | 单元格3 |
| 左对齐 | 居中对齐 | 右对齐 |
| :--- | :---: | ---: |
```



**编辑器操作：**

- 在 Typora 等编辑器中，可以直接右键插入表格。
- 也可以手动输入表头和分割线（第二行的 `---|---`），编辑器会自动识别并生成表格。按 `Tab` 键可以在单元格间切换。

## 水平线

**语法：**

```
---
***
___
```



**编辑器操作：**

- 输入 `---` 或 `***` 后直接按 `Enter` 即可生成一条水平线。

## 其他

### 转义字符

**语法：**

```
\* 显示星号而不是斜体
```



**说明：** 在 Markdown 有特殊含义的字符前加反斜杠 `\`，可以显示其本身。

### 任务列表

**语法：**

```
- [x] 已完成任务
- [ ] 未完成任务
```



**编辑器操作：** 在列表项中，可以点击复选框切换状态。

### 脚注

**语法：**

```
这是一个脚注[^1]

[^1]: 脚注内容
```



**说明：** 脚注通常会在文档末尾自动呈现。

### 定义列表

**语法：**

```
术语一
: 定义一

术语二
: 定义二
```



## 常用技巧

1. **换行**：在行尾添加两个空格后按 `Enter`，或直接空一行（产生新段落）。
2. **强制换行**：使用 `<br>` HTML标签。
3. **HTML支持**：可以在Markdown中直接使用HTML标签来实现更复杂的格式，如 `<center>居中</center>` 或 `<font color="red">红色文字</font>`。

## 扩展语法（部分编辑器支持）

```
==高亮文本==
H~2~O（下标）
X^2^（上标）
```



Markdown 的简洁性使其成为文档编写、笔记记录和网页内容创作的理想选择。不同的平台（如 GitHub、Typora、VS Code）可能会有自己的扩展语法，建议在使用前了解具体的平台特性

## 彩蛋 ##

官方的Typora是需要花钱激活才能使用的，但是我有免费的，可以私聊我
